## Only needed when the frontend is deployed on a different domain than the backend
## (e.g. frontend on Vercel, backend on Railway). Leave unset for local dev — Vite's
## proxy in vite.config.js already forwards API/socket requests to localhost:4000.
## No trailing slash, e.g.: https://your-backend.up.railway.app
VITE_API_URL=
